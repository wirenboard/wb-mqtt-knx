#pragma once

namespace knx
{
    namespace object
    {
        namespace datapointError
        {
            constexpr auto MQTT_INVALID_INDEX = "Invalid MQTT control index";
            constexpr auto KNX_INVALID_PAYLOAD_SIZE = "Invalid KNX APDU payload size";
        }
    }
}